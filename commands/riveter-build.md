---
name: riveter-build
description: "Build a dataset of rows from a natural-language description, optionally enriching it. Usage: /riveter-build <description of the dataset>"
---

Use the **riveter-build-dataset** skill to build a dataset for: $ARGUMENTS

Follow the skill instructions exactly — start the build, poll for completion, and return the generated rows (and any enriched columns if the user asked for them).
